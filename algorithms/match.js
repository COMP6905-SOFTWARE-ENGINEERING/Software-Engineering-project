function match(projects, students, need_inancial_aid, skills){
    var isContain = false;
    for(var i = 0; i < array.length; i ++){
        if( array[i].toString() == element ){
            isContain = true;
        }
    }
    return isContain;
}
module.exports = match;